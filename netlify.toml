# 站点由 GitHub Actions 统一构建（mkdocs build），本平台仅上传产物，不自行构建。
# 请在 Netlify 后台 Build settings 中清空 Build command（保持为空），Publish directory 设为 site。
[build]
  publish = "site"

# SPA 回退（mkdocs 为静态多页，无需，但保留以备子路径）
[[redirects]]
  from = "/*"
  to = "/:splat"
  status = 200
