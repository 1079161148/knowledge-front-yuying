# Cloudflare Pages 项目配置（通过 wrangler 部署）
# 站点由 GitHub Actions 统一构建（mkdocs build）后由工作流上传 site 目录，本平台不自行构建。
# 若使用 Cloudflare 后台 UI：构建命令直接留空（或填写 `echo skip`），输出目录设为 site。
name = "knowledge-front-yuying"
pages_build_output_dir = "site"
