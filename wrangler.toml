# Cloudflare Pages 项目配置（通过 wrangler 或后台 UI 部署）
# 后台 UI 填写项（等效）：
#   构建命令: npm install && npm run vendor:sync && mkdocs build --strict
#   输出目录: site
name = "knowledge-front-yuying"
pages_build_output_dir = "site"
